# KSeFCli — environment variables
# Copy this file to .env and fill in your values.
#   cp .env.example .env

# ── Timezone ──────────────────────────────────────────────────────────────────
TZ=Europe/Warsaw

# ── KSeFCli ───────────────────────────────────────────────────────────────────
KSEFCLI_TAG=latest
KSEFCLI_PORT=18150
KSEFCLI_HOSTNAME=ksef.example.com

# Basic-auth for Traefik (leave empty to disable).
# Generate with:  htpasswd -nb <user> <password>
# Example result: admin:$$apr1$$xyz...  (dollar signs must be doubled in .env)
KSEFCLI_BASICAUTH_USERS=

# ── Traefik ───────────────────────────────────────────────────────────────────
# Certificate resolver name as defined in your Traefik static config.
# Common values: letsencrypt, cloudflare, selfsigned
TRAEFIK_CERT_RESOLVER=letsencrypt

# ── Ofelia ────────────────────────────────────────────────────────────────────
OFELIA_TAG=latest
