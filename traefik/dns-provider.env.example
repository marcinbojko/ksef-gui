# DNS provider credentials for Traefik ACME DNS challenge.
# Copy this file to traefik/dns-provider.env and fill in your values.
# The file is loaded by the traefik service in docker-compose.yml.
#
# Supported providers and their required variables:
#   https://doc.traefik.io/traefik/https/acme/#providers

# ── Cloudflare ────────────────────────────────────────────────────────────────
# Option A — scoped API token (recommended, create at dash.cloudflare.com/profile/api-tokens)
CF_DNS_API_TOKEN=your_cloudflare_scoped_api_token

# Option B — legacy global API key
# CF_API_EMAIL=you@example.com
# CF_API_KEY=your_global_api_key

# ── Other providers ───────────────────────────────────────────────────────────
# OVH:
# OVH_ENDPOINT=ovh-eu
# OVH_APPLICATION_KEY=...
# OVH_APPLICATION_SECRET=...
# OVH_CONSUMER_KEY=...

# Namecheap:
# NAMECHEAP_API_USER=...
# NAMECHEAP_API_KEY=...

# Route53:
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1
